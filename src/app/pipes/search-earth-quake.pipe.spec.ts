import { SearchEarthQuakePipe } from './search-earth-quake.pipe';

describe('SearchEarthQuakePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEarthQuakePipe();
    expect(pipe).toBeTruthy();
  });
});
